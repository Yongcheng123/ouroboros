# 设计说明

这份文档记录**为什么这样设计**，而不是**怎么用**（后者在 README）。
它也是网站上「护栏」一节的来源。

---

## 1. 为什么是装箱问题

一个自我改进的演示，最容易滑向的失败模式是**无法证伪**。
"今天的文案比昨天更好"这种说法没有裁决方式，于是所有的进化都变成了表演。

装箱问题被选中，是因为它同时满足四个条件：

- **指标唯一且客观。** 用掉的箱子数比理论下界多百分之几。没有解释余地。
- **评测极快。** 一个候选在 8 个实例、4000 件物品上跑完只要不到一毫秒，
  所以一晚可以真的搜上万个候选，而不是象征性地搜十个。
- **基线是硬的。** First Fit、Best Fit、Worst Fit 是教科书算法，不是我随手写的稻草人。
  第 0 代直接就是 Best Fit，所以"进化出来的东西"从一开始就要和一个体面的对手比。
- **在线约束让它不平凡。** 物品逐件到达、放了不能动，这排除了"先排序"这类
  把问题做没了的解法。离线的 Best Fit Decreasing 被放在成绩单里当参照，
  但它不在同一个赛道上。

## 2. 基因型为什么是表达式树而不是代码

最直接的做法是让系统生成 JavaScript 函数然后 `eval`。我没有这么做，原因有两条：

**可审计。** 整个个体是一行 JSON，四五十个节点封顶，可以完整塞进一次 `git diff`。
一个没人读得懂的进化产物，讨论价值接近零。网站上把表达式化简之后用中缀形式印出来，
就是为了让人能对着它想"它到底在赌什么"。

**信任边界具体化。** `expr.mjs` 里的 `valid()` 是一个白名单校验器：
11 个算子、4 个变量，别的都不接受。任何候选 —— 不管来自变异算子、来自语言模型、
还是来自未来某个版本的引擎 —— 都必须先过这一关才会被编译。
如果基因型是代码字符串，这条边界就不存在了，"让模型提建议"这件事也就没法安全地做。

## 3. 三折划分

这是整个设计里最花心思的地方。

早期版本只有训练集和留出集。结果很典型：训练集一路走低，留出集在第三代之后开始
反向走高。搜索在学实例的指纹，不是学分布。

现在是三折：

- **训练折（8 个实例）** 驱动搜索。每晚上万个候选都在这里打分。它是热路径，所以最小。
- **验证折（4 个实例）** 只有否决权。一个在训练折上赢了的候选，如果在验证折上多用了
  哪怕一个箱子，当场出局。规则是**绝对的**：验证折的成绩只许降不许升，
  `selfcheck` 会逐代检查这条不变量。
- **留出集（12 个实例）** 什么权力都没有，故意做成最大的一折。
  它唯一的作用，是让网站能报出一个系统无权优化的数字。

否决门的效果在日志里能直接看到：某些晚上有几百个候选在训练折上确实更优，
然后全部倒在第二关。那几百次否决就是这套划分当晚全部的产出。

**为什么不把留出集也放进接受条件里。** 因为那样它就不再是留出集了。
一旦某个数字参与了任何决定，它就只能反映"搜索有多擅长优化它"，而不是"结果有多可靠"。
这是最容易犯、也最不容易被人发现的作弊。

## 4. MAP-Elites 而不是单一最优

只保留一个冠军的话，遗传编程会稳定地长成一坨越来越大、越来越没法读的东西（bloat），
而且一旦陷进局部最优就出不来。

这里按**表达式节点数**分七档，每档各自保留一个最优个体。变异可以从任何一档出发。
这带来三个好处：小表达式和大表达式互不淘汰；搜索有多个起点；
「化简」这个一分适应度都不涨的算子突然变得有意义 —— 个体变短之后会落进更小的档，
在那里可能就是最优。

网站上的精英档案表就是这个网格的直接快照。

## 5. L2：让搜索改进搜索

八个算子的权重不是常数。每晚结束时按这一晚的实测命中率重估：

```
q  <- (1-α)·q + α·(本轮命中率 × 12)
w  <- 地板 + (1 − n·地板) · q / Σq
```

α = 0.18，地板 = 3%。**地板是关键**：它保证没有算子会被永久饿死。
今天没用的思路，等分布或者当前冠军的结构一变，明天可能就是最有效的那个。
一个会把权重归零的更新规则，等于让搜索把自己的一部分能力永久删掉。

同样自适应的还有两样：

- **常数退火步长 σ**：命中率高就放大（探索），低就收缩（精修）。1/5 法则的变体。
- **每晚评估预算**：一晚没找到东西，下一晚 ×1.4；找到了就 ×0.9。
  这一条的方向很重要 —— **搜不出结果时先加算力，而不是先放宽验收标准**。
  后者是同一个困境下更省事、也更危险的解法。

## 6. L1+：合成新算子

引擎有 12% 的概率把两个算子串起来用，并单独记账。
如果某一对组合的命中率超过任何单一算子的 1.25 倍（且至少试过 60 次），
这对组合就被固化成一个有名字的新算子，加进算子表，参与后续的权重竞争。上限四个。

这是这个项目里**搜索空间真的变大了**的唯一一处。它被允许，是因为新算子的每个零件
都是审过的 —— 合成算子只能是已有算子的有序串联，不能凭空出现新行为。

网站的算子表上，这些行会带一个"第 N 代合成"的标记。

## 7. 确定性

每一代的随机种子由 `hash(日期 + 世代号)` 得到，记在谱系表里。
`tools/replay.mjs` 从上一个提交的 `genome.json` 出发重跑那一代，
必须落到**完全相同**的冠军、完全相同的三折得分。

渲染器也必须确定性：同样的基因组必须产出同样的字节。
`selfcheck` 会渲染两遍、再和磁盘上的文件比对。
这条检查的实际作用是禁止渲染器里出现 `Date.now()`、`Math.random()`
和任何依赖对象遍历顺序的东西。

上一个提交就是上一晚的状态 —— **git 本身就是这个项目的只追加日志**，
没有另建一套存储。

## 8. 自我修改的边界

| 层级 | 可改写 | 谁能改 | 理由 |
|---|---|---|---|
| L0 | 启发式表达式 | 引擎 | 受频次最高、后果最小，有验收测试挡着 |
| L1 | 算子权重 / σ / 算力预算 | 引擎 | 改的是搜索怎么花钱，不是什么算赢；改坏了下一晚自己纠回来 |
| L1+ | 合成组合算子 | 引擎 | 零件都审过，上限四个 |
| L2 | 三折划分、接受阈值、下界计算 | **人** | 让被评的一方改评分规则，是最短的作弊路径 |
| L3 | 引擎源代码 | **人，走 PR** | 见下 |

**为什么 L3 关着。** 技术上完全可以让引擎改自己的源码 —— 加一个补丁生成器、
跑测试、通过就提交。不这么做，是因为一旦引擎可以改 `benchmark.mjs`，
"它变好了"这句话就失去了指代对象：任何指标都可以通过改指标来满足。
关掉 L3 之后，网站上每一个数字都还指向一个引擎碰不到的裁决过程。

这条线的位置是**设计决定**，不是能力上限。把它写在这里，是因为在一个自我改进的
系统里，"哪些地方故意不让它改、为什么"比"它能改多少"重要得多。

## 9. 已知的局限

- **进步会越来越慢。** 搜索空间是固定的表达式语法，天花板由它决定。
  预期是前十代进步明显，之后大部分晚上会是"无改进"。
  这是诚实的结果，日志会照实记录，不做修饰。
- **留出集只有 12 个实例。** 粒度偏粗，单个实例差一个箱子就是约 0.04 个百分点的跳动。
  留出集上的小幅波动不应该被当成信号。
- **验证折的否决是绝对的**，这可能挡掉一些"先退一步再前进两步"的路径。
  这是刻意的保守选择：在一个无人值守的循环里，宁可错过，不可放松。
- **在线装箱的改进幅度天然有限。** 相对 Best Fit 提升一个百分点已经不小，
  别期待数量级的变化。
- **语言模型提议层是可选的，且默认关闭。** 没有它，整个系统零依赖、零成本、
  完全离线可跑。它存在是为了演示提议/裁决分离，不是为了提升性能。
